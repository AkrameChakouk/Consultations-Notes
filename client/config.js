// Configuration du client.
// L'adresse de l'API est écrite ICI et nulle part ailleurs : pour brancher le client
// sur une autre machine (machine virtuelle, conteneur Docker...), on ne change que cette ligne.
const CONFIG = {
  API_URL: "http://127.0.0.1:5000"
};
